import { useState } from "react";
import { auth, db } from '../firebase';

export default function SendMessage({scroll, }: {
    scroll: {
        current: {scrollIntoView: (arg0: {behavior: }}
    }
})
